import { TimeMoviePipe } from './time-movie.pipe';

describe('TimeMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
